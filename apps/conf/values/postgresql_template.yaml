global:
  postgresql:
    auth:
      username: "temporal"
      database: "temporal"
      existingSecret: "{{ .postgresSecretName }}"
      secretKeys:
        adminPasswordKey: "postgres-password"
        userPasswordKey: "password"