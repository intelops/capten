ClusterSecretStore:
  create: false
  vault:
    server: http://vault.{{.DomainName}}
    path: secret
    version: v2
    auth:
      secretname: vault-cluster-token
      key: token
      namespace: external-secrets
