image:
  repository: ghcr.io/intelops/kubviz/client
  pullPolicy: Always
  tag: "v1.1.3-alpha"
nats:
  auth:
    token: "{{ .NATSToken }}"
migration:
  image:
    repository: ghcr.io/intelops/kubviz/migration
    pullPolicy: Always
    tag: "v1.1.3-alpha"
